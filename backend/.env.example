# Copy this file to `.env` in the backend/ folder.

# Postgres connection string. Default assumes a local Postgres with no
# password, database named Text_to_Image, current OS user as the DB user
# (standard Homebrew Postgres setup on Mac).
DATABASE_URL=postgresql://localhost/Text_to_Image
